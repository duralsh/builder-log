---
import { getCollection } from "astro:content";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { formatDate, sortByDateDesc } from "../../lib/posts";

const lifting = sortByDateDesc(
  await getCollection("posts", ({ data }) => !data.draft && data.tags.includes("lifting")),
);
const coins = sortByDateDesc(await getCollection("posts", ({ data }) => !data.draft && data.tags.includes("coins")));
---

<BaseLayout title="Hobbies | Dogus Ural">
  <section class="section">
    <div class="container">
      <h1>🧩 Hobbies</h1>
      <p class="muted">Training logs, numismatics notes, and a reading shelf.</p>
    </div>
  </section>

  <section class="section">
    <div class="container hobby-grid">
      <article class="card hobby-block">
        <h2><a href="/hobbies/lifting">🏋️ Lifting</a></h2>
        <p>I am a big fan of kettlebells, boxing, cycling and home workouts.</p>
        <ul>
          {lifting.slice(0, 3).map((post) => (
            <li>
              <a href={`/posts/${post.slug}`}>{post.data.title}</a> <span class="muted">· {formatDate(post.data.date)}</span>
            </li>
          ))}
        </ul>
      </article>

      <article class="card hobby-block">
        <h2><a href="/hobbies/coins">🪙 Numismatics</a></h2>
        <p>I mostly collect Classical Greek tetradrachms and Roman Republic silver denarii.</p>
        <p>
          <a href="/hobbies/coins/collection">Numismatic cabinet →</a>
        </p>
        <ul>
          {coins.slice(0, 3).map((post) => (
            <li>
              <a href={`/posts/${post.slug}`}>{post.data.title}</a> <span class="muted">· {formatDate(post.data.date)}</span>
            </li>
          ))}
        </ul>
      </article>

      <article class="card hobby-block">
        <h2><a href="/hobbies/reading">📚 Reading</a></h2>
        <p><a href="/hobbies/reading/downloads">Downloadable PDFs</a></p>
      </article>
    </div>
  </section>
</BaseLayout>

<style>
  .hobby-grid {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(270px, 1fr));
  }

  .hobby-block {
    padding: 1.2rem;
  }

</style>
