---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { geoConicEqualArea, geoGraticule, geoPath } from "d3-geo";
import { feature } from "topojson-client";
import landTopo from "world-atlas/land-50m.json";

const coins = [
  {
    title: "MACEDON under Roman Protectorate, First Meris",
    type: "Tetradrachm",
    date: "Circa 167-148 BC",
    mint: "Amphipolis",
    obv: "Diademed and draped bust of Artemis right, with bow and quiver over shoulder; all in centre of Macedonian shield.",
    rev: "MAKEΔONΩN / ΠPΩTHΣ. Club; AP monogram above; all within oak wreath; thunderbolt to left.",
    refs: "SNG Copenhagen 1310-1; HGC 3.1, 1103.",
    notes: "Ex Numismatik Naumann, Auction 159 (8 December 2025), lot 138.",
    condition: "Very fine",
    weight: "16.91 g",
    diameter: "30 mm",
    axis: "",
    image: "/coins/macedon-first-meris-tetradrachm.png",
    width: 800,
    height: 424,
  },
  {
    title: "ATTICA, Athens",
    type: "Tetradrachm",
    date: "Circa 454-404 BC",
    mint: "Athens",
    obv: "Head of Athena to right, wearing crested Attic helmet ornamented with three olive leaves above visor and spiral palmette on bowl, round earring with central boss, and pearl necklace.",
    rev: "Owl standing to right with head facing, olive sprig and crescent behind, AΘE before; all within incuse square.",
    refs: "Kroll 8; Dewing 1591-8; SNG Copenhagen 31; HGC 4, 1597.",
    notes: "Ex Savoca Coins, Herakles 4 (15 December 2025), lot 17.",
    condition: "Very fine, few scratches on obv",
    weight: "17.15 g",
    diameter: "23 mm",
    axis: "",
    image: "/coins/athens-tetradrachm-classical.png",
    width: 800,
    height: 403,
  },
  {
    title: "KINGS OF THRACE, Kalchedon, in the name of Lysimachos (305-281 BC)",
    type: "Tetradrachm AR",
    date: "305-281 BC",
    mint: "Kalchedon",
    obv: "Head of the deified Alexander right, wearing horn of Ammon.",
    rev: "BAΣIΛEΩΣ ΛYΣIMAXOY, Athena seated left, holding Nike and spear over shoulder, resting elbow on shield. Controls: grain ear and monogram.",
    refs: "cf. Muller 251; HGC 7, 526.",
    notes: "Ex Savoca Coins, 302nd Monthly Silver Auction (11 January 2026), lot 49.",
    condition: "Extremely fine",
    weight: "17.03 g",
    diameter: "30 mm",
    axis: "",
    image: "/coins/lysimachos-kalchedon-tetradrachm.png",
    width: 800,
    height: 421,
  },
  {
    title: "KINGS OF PERGAMON, Eumenes I (in the name of Philetairos)",
    type: "AR Tetradrachm",
    date: "263-241 BC",
    mint: "Pergamon",
    obv: "Laureate head of Philetairos right.",
    rev: "ΦIΛETAIPOY. Athena seated left, elbow resting on shield to right, holding spear and crowning dynastic name with wreath. Ivy leaf to outer left, A to inner left, bow to right.",
    refs: "SNG BN 1610-2.",
    notes: "Rare obverse variety with slight drapery. Ex Concordia Numismatic, Auction 38 (16 January 2026), lot 380.",
    condition: "Very fine",
    weight: "17.00 g",
    diameter: "30.45 mm",
    axis: "",
    image: "/coins/eumenes-i-philetairos-tetradrachm.png",
    width: 800,
    height: 412,
  },
  {
    title: "KINGDOM OF MACEDON, Alexander III 'the Great' (336-323 BC)",
    type: "AR Drachm",
    date: "Circa 324-323 BC (lifetime issue)",
    mint: "Sardes",
    obv: "Head of Herakles right, wearing lion skin.",
    rev: "Zeus Aetophoros seated left; club upward to outer right.",
    refs: "Price 2561; Muller 137.",
    notes: "Lifetime issue. Ex Hirsch sale 272 (2011), lot 210.",
    condition: "About extremely fine, nicely toned",
    weight: "4.28 g",
    diameter: "17 mm",
    axis: "",
    image: "/coins/alexander-iii-drachm-lifetime-sardes.png",
    width: 685,
    height: 345,
  },
  {
    title: "IONIA, Miletos",
    type: "Obol or Hemihekte",
    date: "Late 6th-early 5th centuries BC",
    mint: "Miletos",
    obv: "Forepart of lion right, head left.",
    rev: "Stellate pattern within incuse square.",
    refs: "SNG Kayhan I 476-82.",
    notes: "Ex Hera Numismatics, Auction 12 (19 December 2025), lot 141.",
    condition: "Good very fine",
    weight: "1.09 g",
    diameter: "8.89 mm",
    axis: "",
    image: "/coins/miletos-obol-hemihekte.png",
    width: 683,
    height: 356,
  },
  {
    title: "MYSIA, Kyzikos",
    type: "Obol",
    date: "Circa 525-475 BC",
    mint: "Kyzikos",
    obv: "Forepart of boar left; tunny to right.",
    rev: "Head of roaring lion left.",
    refs: "SNG France 369-372.",
    notes: "Ex Hera Numismatics, Auction 12 (19 December 2025), lot 144.",
    condition: "Good very fine",
    weight: "1.19 g",
    diameter: "8.65 mm",
    axis: "",
    image: "/coins/kyzikos-obol.png",
    width: 729,
    height: 362,
  },
  {
    title: "HADRIAN (117-138)",
    type: "Denarius",
    date: "Travel Series issue",
    mint: "Rome",
    obv: "HADRIANVS AVG COS III P P. Laureate bust right, with drapery on far shoulder.",
    rev: "AFRICA. Africa, wearing elephant skin headdress and holding scorpion and cornucopia, reclining left on rock; basket of grain ears to left.",
    refs: "RIC II 1494.",
    notes: "Ex Hera Numismatics, Auction 12 (19 December 2025), lot 370.",
    condition: "Good very fine",
    weight: "3.23 g",
    diameter: "16.71 mm",
    axis: "",
    image: "/coins/hadrian-africa-denarius.png",
    width: 800,
    height: 424,
  },
  {
    title: "MAXIMINUS I THRAX (AD 235-238)",
    type: "AR Denarius",
    date: "AD 235-238",
    mint: "Rome",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 1112.",
    condition: "Not specified",
    weight: "2.80 g",
    diameter: "20.06 mm",
    axis: "",
    image: "/coins/maximinus-i-thrax-denarius.png",
    width: 800,
    height: 398,
  },
  {
    title: "ISLANDS off KARIA, Rhodos",
    type: "AR Diobol",
    date: "250 BC",
    mint: "Rhodos",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 379.",
    condition: "Not specified",
    weight: "0.76 g",
    diameter: "10.32 mm",
    axis: "",
    image: "/coins/rhodos-diobol-250bc.png",
    width: 800,
    height: 429,
  },
  {
    title: "THRACE, Chersonesos",
    type: "AR Hemiobol",
    date: "Circa 500 BC",
    mint: "Chersonesos",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 479.",
    condition: "Not specified",
    weight: "2.32 g",
    diameter: "12.51 mm",
    axis: "",
    image: "/coins/chersonesos-hemiobol-500bc.png",
    width: 800,
    height: 396,
  },
  {
    title: "PISIDIA, Selge",
    type: "AR Obol",
    date: "Circa 350-300 BC",
    mint: "Selge",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 887.",
    condition: "Not specified",
    weight: "0.81 g",
    diameter: "9.77 mm",
    axis: "",
    image: "/coins/selge-obol-350-300bc.png",
    width: 800,
    height: 410,
  },
  {
    title: "DYNASTS of LYCIA, uncertain",
    type: "AR Obol",
    date: "Circa 6th-5th century BC",
    mint: "Lycia",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 888.",
    condition: "Not specified",
    weight: "0.83 g",
    diameter: "9.36 mm",
    axis: "",
    image: "/coins/lycia-dynasts-obol-6th-5thbc.png",
    width: 800,
    height: 421,
  },
  {
    title: "PISIDIA, Selge",
    type: "AR Obol",
    date: "Ca. 4th century BC",
    mint: "Selge",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 893.",
    condition: "Not specified",
    weight: "0.92 g",
    diameter: "10.00 mm",
    axis: "",
    image: "/coins/selge-obol-4thbc-a.png",
    width: 800,
    height: 411,
  },
  {
    title: "PISIDIA, Selge",
    type: "AR Obol",
    date: "Ca. 4th century BC",
    mint: "Selge",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 904.",
    condition: "Not specified",
    weight: "1.03 g",
    diameter: "10.31 mm",
    axis: "",
    image: "/coins/selge-obol-4thbc-b.png",
    width: 800,
    height: 408,
  },
  {
    title: "CARIA, Rhodes",
    type: "AR Hemidrahm or Diobol",
    date: "Circa 205-188 BC",
    mint: "Rhodes",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 913.",
    condition: "Not specified",
    weight: "1.12 g",
    diameter: "11.20 mm",
    axis: "",
    image: "/coins/rhodes-hemidrahm-diobol-205-188bc-a.png",
    width: 800,
    height: 420,
  },
  {
    title: "CARIA, Rhodes",
    type: "AR Hemidrahm or Diobol",
    date: "Circa 205-188 BC",
    mint: "Rhodes",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 922.",
    condition: "Not specified",
    weight: "1.24 g",
    diameter: "13.36 mm",
    axis: "",
    image: "/coins/rhodes-hemidrahm-diobol-205-188bc-b.png",
    width: 800,
    height: 404,
  },
  {
    title: "IONIA, Phokaia",
    type: "AR Diobol",
    date: "Circa 521-478 BC",
    mint: "Phokaia",
    obv: "Not specified.",
    rev: "Not specified.",
    refs: "Reference not specified.",
    notes: "Ex Concordia Numismatic, Daphne Auction 37 (10 January 2026), lot 923.",
    condition: "Not specified",
    weight: "1.25 g",
    diameter: "11.22 mm",
    axis: "",
    image: "/coins/phokaia-diobol-521-478bc.png",
    width: 800,
    height: 464,
  },
  {
    title: "ANTONINUS PIUS",
    type: "Denarius",
    date: "AD 157-158",
    mint: "Rome",
    obv: "ANTONINVS AVG PIVS P P, laureate head right.",
    rev: "VOTA SVSCEPTA DEC III, Antoninus standing left, sacrificing out of patera over tripod altar; COS IIII in exergue.",
    refs: "RIC 283; BMC 585.",
    notes: "Ex AGORA Wien, Auction 1 (15 December 2025), lot 93.",
    condition: "Extremely fine",
    weight: "3.47 g",
    diameter: "19 mm",
    axis: "12 h",
    image: "/coins/antoninus-pius-denarius-157-158.png",
    width: 800,
    height: 413,
  },
  {
    title: "MARCUS AURELIUS",
    type: "AR Denarius",
    date: "AD 162",
    mint: "Rome",
    obv: "Bare head right, slight drapery.",
    rev: "Providentia standing left, holding globe and cornucopia.",
    refs: "RIC 48 var. (slight drapery); RSC 517.",
    notes: "Purchased from Savoca Numismatik, Munchen.",
    condition: "About extremely fine, nice patina",
    weight: "3.49 g",
    diameter: "19 mm",
    axis: "",
    image: "/coins/marcus-aurelius-denarius-ad-162-providentia.png",
    width: 763,
    height: 355,
  },
  {
    title: "MARCUS AURELIUS",
    type: "AR Denarius",
    date: "AD 175-176",
    mint: "Rome",
    obv: "Laureate head right.",
    rev: "Aequitas standing left, holding scales and cornucopia.",
    refs: "RIC 359 var. (reverse legend); RSC 929c.",
    notes: "Purchased from Savoca Numismatik, Munchen.",
    condition: "Extremely fine, nice patina",
    weight: "3.44 g",
    diameter: "19 mm",
    axis: "",
    image: "/coins/marcus-aurelius-denarius-ad-175-176-aequitas.png",
    width: 756,
    height: 379,
  },
  {
    title: "GORDIAN III (238-244)",
    type: "Antoninianus",
    date: "AD 241-243",
    mint: "Rome",
    obv: "IMP GORDIANVS PIVS FEL AVG, radiate, draped and cuirassed bust of Gordian III to right, seen from behind.",
    rev: "VIRTVTI AVGVSTI, Hercules, laureate, standing right, holding his right hand behind his back and leaning left on club set on rock; lion's skin draped over club.",
    refs: "RIC 95; Cohen 404; Michaux 385.",
    notes: "Ex AGORA Wien, Auction 1 (15 December 2025), lot 126.",
    condition: "About extremely fine",
    weight: "4.05 g",
    diameter: "22 mm",
    axis: "2 h",
    image: "/coins/gordian-iii-antoninianus.png",
    width: 800,
    height: 416,
  },
  {
    title: "VALERIAN I",
    type: "BI Antoninianus",
    date: "AD 253-260",
    mint: "Antioch",
    obv: "IMP C P LIC VALERIANVS AVG, radiate, draped and cuirassed bust right.",
    rev: "VIRTVS AVGG, Valerian and Gallienus facing each other, each holding spear, one holding Victory and the other a globe.",
    refs: "RIC 292c; Cohen 277.",
    notes: "Ex AGORA Wien, Auction 1 (15 December 2025), lot 144.",
    condition: "Near extremely fine",
    weight: "3.96 g",
    diameter: "22 mm",
    axis: "",
    image: "/coins/valerian-i-bi-antoninianus.png",
    width: 800,
    height: 379,
  },
  {
    title: "CONSTANTINE I 'the Great' (307-337)",
    type: "Follis (Bronze)",
    date: "AD 315-316",
    mint: "Lugdunum (Lyons)",
    obv: "IMP CONSTANTINVS AVG, laureate, draped and cuirassed bust right, seen from behind.",
    rev: "SOLI INVICTO COMITI, Sol, radiate, standing left, holding globe and raising right hand; TF - star across fields. Mintmark PLG.",
    refs: "RIC VII Lyons 32.",
    notes: "Ex AGORA Wien, Auction 1 (15 December 2025), lot 207.",
    condition: "Extremely fine",
    weight: "4.11 g",
    diameter: "20 mm",
    axis: "6 h",
    image: "/coins/constantine-i-follis-lyons.png",
    width: 800,
    height: 413,
  },
  {
    title: "CONSTANTINE I 'the Great' (307-337)",
    type: "Follis (Bronze)",
    date: "AD 318",
    mint: "Siscia, 4th officina",
    obv: "IMP CONSTANTINVS PF AVG, helmeted, laureate, cuirassed bust right.",
    rev: "VICTORIAE LAETAE PRINC PERP, two Victories facing and inscribing VOT PR on shield placed on altar. Mintmark ΔSIS star.",
    refs: "RIC VII Siscia 47.",
    notes: "Ex AGORA Wien, Auction 1 (15 December 2025), lot 209.",
    condition: "Very fine / choice very fine",
    weight: "3.78 g",
    diameter: "19 mm",
    axis: "1 h",
    image: "/coins/constantine-i-follis-siscia.png",
    width: 800,
    height: 431,
  },
  {
    title: "NERVA (96-98)",
    type: "Denarius",
    date: "AD 96-98",
    mint: "Rome",
    obv: "IMP NERVA CAES AVG P M TR P COS III P P. Laureate head right.",
    rev: "CONCORDIA EXERCITVVM. Clasped hands.",
    refs: "RIC 14.",
    notes: "Ex Numismatik Naumann, Auction 160 (4 January 2026), lot 637.",
    condition: "Very fine",
    weight: "3.75 g",
    diameter: "18 mm",
    axis: "",
    image: "/coins/nerva-denarius-concordia-exercitvvm.png",
    width: 800,
    height: 423,
  },
  {
    title: "TRAJAN (AD 98-117)",
    type: "AR Denarius",
    date: "AD 98-117",
    mint: "Rome",
    obv: "IMP CAES NER TRAIAN OPTIM AVG GER DAC PARTHICO. Laureate bust of Trajan right, with slight drapery.",
    rev: "P M TR P COS VI P P S P Q R, PROVID. Providentia standing left, leaning upon column to right, holding sceptre and pointing toward globus to left.",
    refs: "RIC 364; Woytek 562d.",
    notes: "Rare obverse variety with slight drapery. Ex Concordia Numismatic, Auction 38 (16 January 2026), lot 458.",
    condition: "Very fine",
    weight: "3.29 g",
    diameter: "17.86 mm",
    axis: "",
    image: "/coins/trajan-providentia-denarius.png",
    width: 799,
    height: 396,
  },
  {
    title: "CORINTHIA, Corinth",
    type: "Stater",
    date: "Circa 400-375 BC",
    mint: "Corinth",
    obv: "Pegasos flying right, koppa below.",
    rev: "Head of Athena right, wearing laureate Corinthian helmet; coiled serpent to left.",
    refs: "Pegasi 222 var. (no laurel-wreath); BCD Corinth 63 var. (same); HGC 4, 1834.",
    notes: "Ex Numismatik Naumann, Auction 160 (4 January 2026), lot 168.",
    condition: "Very fine",
    weight: "8.54 g",
    diameter: "22 mm",
    axis: "",
    image: "/coins/corinth-stater-pegasos-athena.png",
    width: 800,
    height: 408,
  },
  {
    title: "ARMENIA, Levon I (1198-1219)",
    type: "Tram",
    date: "1198-1219",
    mint: "Armenia",
    obv: "Crowned figure of Levon seated on throne ornamented with lions, holding cross and fleur-de-lis, feet on footstool.",
    rev: "Two lions rampant, back to back; long cross between.",
    refs: "Cf. AC 282-3 (for type).",
    notes: "Ex Numismatik Naumann, Auction 159 (8 December 2025), lot 1031.",
    condition: "Good very fine",
    weight: "2.87 g",
    diameter: "24 mm",
    axis: "",
    image: "/coins/armenia-levon-i-tram.png",
    width: 800,
    height: 425,
  },
  {
    title: "CARACALLA (198-217)",
    type: "Denarius",
    date: "AD 212",
    mint: "Rome",
    obv: "ANTONINVS PIVS AVG BRIT, laureate head of Caracalla right.",
    rev: "PM TR P XV COS III P P, Annona seated left, holding grain ears and cornucopiae, modius to left.",
    refs: "RIC 195; RSC 205.",
    notes: "Ex AGORA Wien, Auction 2 (4 June 2026), lot 74.",
    condition: "About extremely fine / choice very fine",
    weight: "2.49 g",
    diameter: "19 mm",
    axis: "12 h",
    image: "/coins/caracalla-denarius-ad-212-annona.jpg",
    width: 2008,
    height: 1022,
  },
  {
    title: "SELEUCIS AND PIERIA, Antioch. Philip I 'the Arab' (244-249)",
    type: "BI Tetradrachm",
    date: "AD 244-249",
    mint: "Antioch",
    obv: "AYTOK K M IOYΛ ΦIΛIΠΠOY CEB, radiate, draped and cuirassed bust right.",
    rev: "ΔHMAPX EΞOYCIAC YΠATO A, S C in exergue. Eagle standing left on palm branch, head left, wings spread, wreath in beak.",
    refs: "Prieur 339.",
    notes: "Ex Elegius Numismatik, Web Auction 4 (25 August 2026), lot 183.",
    condition: "Extremely fine",
    weight: "10.51 g",
    diameter: "26.27 mm",
    axis: "",
    image: "/coins/philip-i-tetradrachm-antioch.jpg",
    width: 1664,
    height: 798,
  },
  {
    title: "LICINIUS I (308-324)",
    type: "Æ Follis",
    date: "AD 313",
    mint: "Heraclea",
    obv: "IMP C VAL LICIN LICINIVS P F AVG, laureate head of Licinius right.",
    rev: "IOVI CONSERVATORI AVGG, Jupiter standing left, holding sceptre and Victory on globe, eagle with laurel wreath in beak below; Δ in right field, SMHT in exergue.",
    refs: "RIC 73.",
    notes: "Ex AGORA Wien, Auction 2 (4 June 2026), lot 142.",
    condition: "Choice extremely fine, matte light-brown patina, unusually well centred",
    weight: "3.72 g",
    diameter: "22 mm",
    axis: "5 h",
    image: "/coins/licinius-i-follis-heraclea.jpg",
    width: 2444,
    height: 1209,
  },
  {
    title: "CONSTANTINE I 'the Great' (306-337)",
    type: "Æ Nummus",
    date: "AD 318-319",
    mint: "Ticinum (1st officina)",
    obv: "IMP CONSTANTINVS MAX AVG, laureate, helmeted and cuirassed bust right.",
    rev: "VICTORIAE LAETAE PRINC PERP, two Victories standing facing, together holding shield inscribed VOT PR on altar; PT in exergue.",
    refs: "RIC 82.",
    notes: "Ex AGORA Wien, Auction 2 (4 June 2026), lot 149.",
    condition: "Extremely fine / choice very fine",
    weight: "2.11 g",
    diameter: "18 mm",
    axis: "12 h",
    image: "/coins/constantine-i-nummus-ticinum.jpg",
    width: 1870,
    height: 968,
  },
  {
    title: "CONSTANTINE I 'the Great' (306-337)",
    type: "Æ Nummus",
    date: "AD 321-324",
    mint: "Nicomedia (2nd officina)",
    obv: "IMP C FL VAL CONSTANTINVS P F AVG, radiate, draped and cuirassed bust right.",
    rev: "IOVI CONSERVATORI, Jupiter standing left, holding Victory on globe and eagle-tipped sceptre; X IIΓ above captive on ground right, eagle to left holding wreath in beak; SMNB in exergue.",
    refs: "RIC VII 43.",
    notes: "Ex AGORA Wien, Auction 2 (4 June 2026), lot 166.",
    condition: "Extremely fine, well centred, dark green patina",
    weight: "2.96 g",
    diameter: "20 mm",
    axis: "12 h",
    image: "/coins/constantine-i-nummus-nicomedia.jpg",
    width: 2110,
    height: 1004,
  },
  {
    title: "ATTICA, Athens",
    type: "Tetradrachm",
    date: "Circa 454-404 BC",
    mint: "Athens",
    obv: "Head of Athena right, wearing crested Attic helmet ornamented with three olive leaves above visor and spiral palmette on bowl, round earring and pearl necklace.",
    rev: "Owl standing right, head facing; olive sprig and crescent behind, AΘE before; all within incuse square.",
    refs: "Kroll 8; HGC 4, 1597.",
    notes: "Ex Numismatik Naumann, Auction 159 (8 December 2025), lot 152.",
    condition: "Not specified",
    weight: "Not specified",
    diameter: "Not specified",
    axis: "",
    image: "/coins/athens-tetradrachm-owl.png",
    width: 800,
    height: 438,
  },
  {
    title: "ANTONINUS PIUS with MARCUS AURELIUS as Caesar",
    type: "Denarius",
    date: "AD 139-161",
    mint: "Rome",
    obv: "ANTONINVS AVG PIVS P P, laureate head of Antoninus Pius right.",
    rev: "AVRELIVS CAESAR AVG PII F COS II, bare head of Marcus Aurelius right.",
    refs: "Cf. RIC III 417a; RSC 15.",
    notes: "Ex Numismatik Naumann, Auction 159 (8 December 2025), lot 704.",
    condition: "Not specified",
    weight: "Not specified",
    diameter: "Not specified",
    axis: "",
    image: "/coins/antoninus-pius-marcus-aurelius-denarius.png",
    width: 800,
    height: 401,
  },
  {
    title: "BAKTRIA, Sophytes (Sophytos)",
    type: "AR Drachm",
    date: "Circa 4th-3rd century BC",
    mint: "Uncertain mint, Baktria",
    obv: "Head right wearing a close-fitting helmet bound with a laurel wreath and fitted with a cheek guard.",
    rev: "ΣΩΦΥΤΟΥ, cock standing right; caduceus in the left field.",
    refs: "Cf. Bopearachchi, Sophytes; HGC 12, 13.",
    notes: "Photographed by me; no purchase record. Attribution of the Sophytes series is long debated — the coins have been placed variously with the Punjab ruler who submitted to Alexander, with a figure based in Baktria, and with a Mauryan satrap in Arachosia.",
    condition: "Struck on a thick flan",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/sophytes-drachm.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Euthydemos I (225-200 BC)",
    type: "AR Tetradrachm",
    date: "Circa 225-200 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed head of Euthydemos right, within bead border.",
    rev: "BAΣIΛEΩΣ / EYΘYΔHMOY, Herakles seated left on rocks, holding club resting on rock; monogram in left field.",
    refs: "Bopearachchi 1; HGC 12, 44.",
    notes: "Photographed by me; no purchase record.",
    condition: "Old cabinet tone",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/euthydemos-i-tetradrachm-a.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Euthydemos I (225-200 BC)",
    type: "AR Tetradrachm",
    date: "Circa 225-200 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed head of Euthydemos right, within bead border.",
    rev: "BAΣIΛEΩΣ / EYΘYΔHMOY, Herakles seated left on rocks, holding club; monogram in left field.",
    refs: "Bopearachchi 1; HGC 12, 44.",
    notes: "Photographed by me; no purchase record.",
    condition: "Dark cabinet tone",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/euthydemos-i-tetradrachm-b.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Euthydemos I (225-200 BC)",
    type: "AR Tetradrachm",
    date: "Circa 225-200 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed head of Euthydemos right, within bead border.",
    rev: "BAΣIΛEΩΣ / EYΘYΔHMOY, Herakles seated left on rocks, holding club; monogram in left field.",
    refs: "Bopearachchi 1; HGC 12, 44.",
    notes: "Photographed by me; no purchase record.",
    condition: "Broad flan, dark tone",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/euthydemos-i-tetradrachm-c.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Demetrios I Aniketos (200-185 BC)",
    type: "AR Drachm",
    date: "Circa 200-185 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed and draped bust right wearing elephant-scalp headdress.",
    rev: "BAΣIΛEΩΣ / ΔHMHTPIOY, Herakles standing facing, crowning himself, holding club and lion skin; monogram K in left field.",
    refs: "Cf. Bopearachchi 1-2; HGC 12, 67.",
    notes: "Photographed by me; no purchase record.",
    condition: "",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/demetrios-i-drachm.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Antimachos I Theos (174-165 BC)",
    type: "AR Tetradrachm",
    date: "Circa 174-165 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed and draped bust right wearing flat kausia.",
    rev: "BAΣIΛEΩΣ / ΘEOY, ANTIMAXOY in exergue. Poseidon standing facing, holding trident and palm; monogram in field.",
    refs: "Bopearachchi 1; HGC 12, 106.",
    notes: "Photographed by me; no purchase record.",
    condition: "Large flan, old flan crack",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/antimachos-i-theos-tetradrachm.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Eukratides I Megas (171-145 BC)",
    type: "AR Tetradrachm",
    date: "Circa 171-145 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed and draped bust right wearing crested Boiotian helmet adorned with bull's horn and ear, within bead-and-reel border.",
    rev: "BAΣIΛEΩΣ MEΓAΛOY, EYKPATIΔOY in exergue. The Dioskouroi on horseback charging right, holding spears and palms; monogram in right field.",
    refs: "Bopearachchi 6; SNG ANS 465ff; HGC 12, 131.",
    notes: "Photographed by me; no purchase record.",
    condition: "Bright surfaces, well centred",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/eukratides-i-tetradrachm-a.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Eukratides I Megas (171-145 BC)",
    type: "AR Tetradrachm",
    date: "Circa 171-145 BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed and draped bust right wearing crested Boiotian helmet adorned with bull's horn and ear.",
    rev: "BAΣIΛEΩΣ MEΓAΛOY, EYKPATIΔOY in exergue. The Dioskouroi on horseback charging right, holding spears and palms; monogram in right field.",
    refs: "Bopearachchi 6; SNG ANS 465ff; HGC 12, 131.",
    notes: "Photographed by me; no purchase record.",
    condition: "Granular surfaces, edge encrustation",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/eukratides-i-tetradrachm-b.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, uncertain king",
    type: "AR Drachm",
    date: "2nd century BC",
    mint: "Uncertain mint, Baktria",
    obv: "Diademed bust right; legend off flan.",
    rev: "Standing deity; legend largely effaced.",
    refs: "Not identified.",
    notes: "Ruler not identified — legend illegible at this state of wear. Photographed by me; no purchase record.",
    condition: "Heavily worn",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/baktria-uncertain-drachm.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "INDO-GREEK KINGDOM, Antimachos II Nikephoros (174-165 BC)",
    type: "AR Drachm",
    date: "Circa 174-165 BC",
    mint: "Uncertain mint, Indo-Greek realm",
    obv: "Diademed bust right.",
    rev: "BAΣIΛEΩΣ NIKHΦOPOY / ANTIMAXOY, Nike standing left holding wreath and palm; Kharoshthi legend around; monogram in field.",
    refs: "Bopearachchi 1; HGC 12, 117.",
    notes: "Photographed by me; no purchase record.",
    condition: "",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/antimachos-ii-drachm.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "INDO-GREEK KINGDOM, Menander I Soter (165-130 BC)",
    type: "AR Drachm",
    date: "Circa 165-130 BC",
    mint: "Uncertain mint, Indo-Greek realm",
    obv: "BAΣIΛEΩΣ ΣΩTHPOΣ MENANΔPOY, helmeted and draped bust right wearing crested helmet.",
    rev: "Athena Alkidemos advancing left, hurling thunderbolt and holding aegis; Kharoshthi legend around; monogram in field.",
    refs: "Bopearachchi 13; HGC 12, 190.",
    notes: "Photographed by me; no purchase record.",
    condition: "Helmeted variety",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/menander-i-drachm-helmeted.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "INDO-GREEK KINGDOM, Menander I Soter (165-130 BC)",
    type: "AR Drachm",
    date: "Circa 165-130 BC",
    mint: "Uncertain mint, Indo-Greek realm",
    obv: "BAΣIΛEΩΣ ΣΩTHPOΣ MENANΔPOY, diademed and draped bust right.",
    rev: "Athena Alkidemos advancing left, hurling thunderbolt and holding aegis; Kharoshthi legend around; monogram in field.",
    refs: "Bopearachchi 12; HGC 12, 188.",
    notes: "Photographed by me; no purchase record.",
    condition: "Sharp legend",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/menander-i-drachm-a.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "INDO-GREEK KINGDOM, Menander I Soter (165-130 BC)",
    type: "AR Drachm",
    date: "Circa 165-130 BC",
    mint: "Uncertain mint, Indo-Greek realm",
    obv: "BAΣIΛEΩΣ ΣΩTHPOΣ MENANΔPOY, diademed and draped bust right.",
    rev: "Athena Alkidemos advancing left, hurling thunderbolt and holding aegis; Kharoshthi legend around; monogram in field.",
    refs: "Bopearachchi 12; HGC 12, 188.",
    notes: "Photographed by me; no purchase record.",
    condition: "",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/menander-i-drachm-b.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "BAKTRIA, Sophytes (Sophytos)",
    type: "AR Obol",
    date: "Circa 4th-3rd century BC",
    mint: "Uncertain mint, Baktria",
    obv: "Head right wearing a close-fitting helmet bound with a laurel wreath.",
    rev: "ΣΩΦΥΤΟΥ, cock standing right; caduceus in the left field.",
    refs: "Cf. Bopearachchi, Sophytes; HGC 12, 15.",
    notes: "Photographed by me; no purchase record. Attribution of the Sophytes series is long debated — the coins have been placed variously with the Punjab ruler who submitted to Alexander, with a figure based in Baktria, and with a Mauryan satrap in Arachosia.",
    condition: "",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/sophytes-obol.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "ABBASID CALIPHATE",
    type: "AR Dirham",
    date: "AD 750-1258",
    mint: "Mint illegible",
    obv: "Kalima in three lines within inner circle: la ilaha illa / Allah wahdahu / la sharika lahu. Marginal legend with the mint-and-date formula.",
    rev: "Legend in field with marginal inscription; annulets in outer margin.",
    refs: "Not identified to type.",
    notes: "Mint and AH year are in the marginal legend and could not be read from the available photographs. Photographed by me; no purchase record.",
    condition: "",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/abbasid-dirham-a.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "ABBASID CALIPHATE",
    type: "AR Dirham",
    date: "AD 750-1258",
    mint: "Mint illegible",
    obv: "Kalima in three lines within inner circle: la ilaha illa / Allah wahdahu / la sharika lahu. Marginal legend with the mint-and-date formula.",
    rev: "Legend in field with marginal inscription; annulets in outer margin.",
    refs: "Not identified to type.",
    notes: "Mint and AH year are in the marginal legend and could not be read from the available photographs. Photographed by me; no purchase record.",
    condition: "",
    weight: "",
    diameter: "",
    axis: "",
    image: "/coins/abbasid-dirham-b.jpg",
    width: 1840,
    height: 940,
  },
  {
    title: "PHILIP I 'the Arab' (244-249)",
    type: "AR Antoninianus",
    date: "AD 244-247",
    mint: "Rome",
    obv: "IMP M IVL PHILIPPVS AVG, radiate, draped and cuirassed bust of Philip I right.",
    rev: "AEQVITAS AVG, Aequitas standing left, holding scales and cornucopia.",
    refs: "RIC 27b.",
    notes: "Ex Mars Vltor, Auction 28 (30 August 2026), lot 187.",
    condition: "About extremely fine",
    weight: "4.29 g",
    diameter: "23 mm",
    axis: "",
    image: "/coins/philip-i-antoninianus-aequitas.jpg",
    width: 1857,
    height: 876,
  },
  {
    title: "PHILIP II (247-249)",
    type: "AR Antoninianus",
    date: "AD 247-249",
    mint: "Rome",
    obv: "IMP PHILIPPVS AVG, radiate, draped and cuirassed bust of Philip II right, seen from behind.",
    rev: "PAX AETERNA, Pax standing facing, head left, holding branch and sceptre.",
    refs: "RIC 231c; Cohen 23.",
    notes: "Ex Mars Vltor, Auction 28 (30 August 2026), lot 191.",
    condition: "About extremely fine; one insignificant tiny crack, excellent metal",
    weight: "4.42 g",
    diameter: "25 mm",
    axis: "",
    image: "/coins/philip-ii-antoninianus-pax.jpg",
    width: 1992,
    height: 904,
  },
  {
    title: "PROBUS (276-282)",
    type: "Silvered Æ Antoninianus",
    date: "AD 277",
    mint: "Antioch",
    obv: "IMP C M AVR PROBVS AVG, radiate, draped and cuirassed bust right.",
    rev: "CLEMENTIA TEMP, emperor standing right holding eagle-tipped sceptre, receiving globe from Jupiter standing left holding sceptre; H in lower centre, XXI in exergue.",
    refs: "RIC 921; Sear 11960.",
    notes: "Ex Mars Vltor, Auction 28 (30 August 2026), lot 258.",
    condition: "Good very fine, fully silvered",
    weight: "4.01 g",
    diameter: "21 mm",
    axis: "",
    image: "/coins/probus-antoninianus-clementia.jpg",
    width: 1681,
    height: 843,
  },
  {
    title: "DIOCLETIAN (284-305)",
    type: "Æ Antoninianus",
    date: "AD 285-286",
    mint: "Ticinum",
    obv: "IMP C C VAL DIOCLETIANVS P F AVG, radiate, draped and cuirassed bust of Diocletian right.",
    rev: "IOVI CONSERVAT, Jupiter standing facing, head left, holding thunderbolt and long sceptre; PXXIT in exergue.",
    refs: "RIC 222; Cohen 201.",
    notes: "Ex Mars Vltor, Auction 28 (30 August 2026), lot 274.",
    condition: "Good very fine, struck on a big, perfectly round flan",
    weight: "4.40 g",
    diameter: "23 mm",
    axis: "",
    image: "/coins/diocletian-antoninianus-iovi.jpg",
    width: 1818,
    height: 939,
  },
  {
    title: "CONSTANTINE I 'the Great' (307-337)",
    type: "Silvered Æ Follis",
    date: "AD 307-337",
    mint: "Siscia",
    obv: "CONSTANTINVS AVG, laureate head of Constantine I right.",
    rev: "PROVIDENTIAE AVGG, camp gate with two beacons, eight layers, dots in top layer, star above; ASIS and double crescent in exergue.",
    refs: "RIC 214; Sear 16253.",
    notes: "Ex Mars Vltor, Auction 28 (30 August 2026), lot 303.",
    condition: "About extremely fine",
    weight: "3.2 g",
    diameter: "18 mm",
    axis: "",
    image: "/coins/constantine-i-follis-siscia-campgate.jpg",
    width: 1506,
    height: 736,
  },
];

// "AD 212" -> 212, "Circa 167-148 BC" -> -167, "Ca. 4th century BC" -> -400.
// Returns null when the string names no year, so callers can fall back.
const parseStartYear = (value: string) => {
  const bc = /BC/i.test(value);
  const century = value.match(/(\d{1,2})(?:st|nd|rd|th)\b/i);
  if (century) {
    const n = Number(century[1]);
    return bc ? -(n * 100) : (n - 1) * 100 + 1;
  }
  const matches = [...value.matchAll(/\d{2,4}/g)].map((m) => Number(m[0]));
  if (matches.length === 0) return null;
  return bc ? -matches[0] : matches[0];
};

// A few entries date the coin only in prose ("Roman Imperial period",
// "Travel Series issue"); those carry the reign years in the title instead.
const yearFor = (coin: (typeof coins)[number]) => parseStartYear(coin.date) ?? parseStartYear(coin.title) ?? 0;

const toToken = (value: string) =>
  value
    .toLowerCase()
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/^-+|-+$/g, "");

const anchorCounts = new Map<string, number>();
const buildAnchor = (value: string) => {
  const base = toToken(value) || "item";
  const next = (anchorCounts.get(base) ?? 0) + 1;
  anchorCounts.set(base, next);
  return next === 1 ? base : `${base}-${next}`;
};

const categoryFor = (coin: (typeof coins)[number]) => {
  // Dynasty rules first: these are decided by who struck the coin, not by the
  // year. An Abbasid dirham of AD 786 would otherwise fall through to "Roman
  // Imperial", and the Baktrian kings to "Classical & Hellenistic Greek".
  if (/ARMENIA|Levon/i.test(coin.title)) return "Medieval";
  if (/^ABBASID/i.test(coin.title)) return "Islamic";
  if (/^BAKTRIA|^INDO-GREEK|SOGDIANA/i.test(coin.title)) return "Greco-Baktrian & Indo-Greek";
  const year = yearFor(coin);
  if (year >= 1) return "Roman Imperial";
  if (year <= -500) return "Archaic Greek";
  if (year < 0) return "Classical & Hellenistic Greek";
  return "Other";
};

const categories = [
  "Archaic Greek",
  "Classical & Hellenistic Greek",
  "Greco-Baktrian & Indo-Greek",
  "Roman Imperial",
  "Islamic",
  "Medieval",
  "Other",
] as const;

const groupedCoins = categories
  .map((category) => ({
    category,
    items: coins
      .filter((coin) => categoryFor(coin) === category)
      .sort((a, b) => yearFor(a) - yearFor(b)),
  }))
  .filter((group) => group.items.length > 0);

// Nothing may vanish. This page once shipped with 7 coins silently filtered out
// because their computed category was absent from the list above; a new coin that
// matches no rule must break the build, not disappear from the page.
const placedCount = groupedCoins.reduce((n, group) => n + group.items.length, 0);
if (placedCount !== coins.length) {
  throw new Error(
    `Cabinet: ${coins.length - placedCount} coin(s) fell outside every listed category (${placedCount}/${coins.length} placed).`,
  );
}
const uncategorised = coins.filter((coin) => categoryFor(coin) === "Other");
if (uncategorised.length > 0) {
  throw new Error(
    `Cabinet: no category rule matched: ${uncategorised.map((coin) => `"${coin.title}" (${coin.date})`).join("; ")}`,
  );
}

// ---------------------------------------------------------------------------
// Mint map. Everything below runs at build time, so the projection, the
// coastline and the marker positions are baked into the HTML as inline SVG —
// no mapping library reaches the browser.
// ---------------------------------------------------------------------------

type Mint = { label: string; lat: number; lon: number; region?: boolean; dx?: number; dy?: number };

const MINTS: Record<string, Mint> = {
  lugdunum: { label: "Lugdunum", lat: 45.76, lon: 4.83 },
  ticinum: { label: "Ticinum", lat: 45.19, lon: 9.16, dy: -10 },
  rome: { label: "Rome", lat: 41.89, lon: 12.49 },
  siscia: { label: "Siscia", lat: 45.49, lon: 16.37 },
  corinth: { label: "Corinth", lat: 37.94, lon: 22.93, dy: 12 },
  athens: { label: "Athens", lat: 37.98, lon: 23.73, dx: 8, dy: 8 },
  amphipolis: { label: "Amphipolis", lat: 40.82, lon: 23.84, dy: -10 },
  chersonesos: { label: "Chersonesos", lat: 40.30, lon: 26.40, dy: 14 },
  phokaia: { label: "Phokaia", lat: 38.67, lon: 26.76, dx: -6, dy: 10 },
  miletos: { label: "Miletos", lat: 37.53, lon: 27.28, dy: 12 },
  pergamon: { label: "Pergamon", lat: 39.12, lon: 27.18, dx: 6, dy: -8 },
  kyzikos: { label: "Kyzikos", lat: 40.39, lon: 27.89, dy: -12 },
  heraclea: { label: "Heraclea", lat: 40.97, lon: 27.95, dy: -22 },
  sardes: { label: "Sardes", lat: 38.49, lon: 28.04, dx: 10 },
  rhodes: { label: "Rhodes", lat: 36.44, lon: 28.22, dy: 14 },
  kalchedon: { label: "Kalchedon", lat: 40.98, lon: 29.03, dx: 14, dy: -10 },
  nicomedia: { label: "Nicomedia", lat: 40.77, lon: 29.92, dx: 16 },
  lycia: { label: "Lycia", lat: 36.30, lon: 29.80, region: true, dy: 14 },
  selge: { label: "Selge", lat: 37.23, lon: 31.13, dy: 12 },
  armenia: { label: "Cilician Armenia", lat: 37.45, lon: 35.82, region: true, dy: -12 },
  antioch: { label: "Antioch", lat: 36.20, lon: 36.16, dy: 14 },
  baktria: { label: "Bactria", lat: 36.76, lon: 66.90, region: true, dy: -12 },
  // Menander I ruled from Sagala (modern Sialkot); his realm ran from the Kabul
  // river east to the Ravi, with attested mints at Taxila and Alexandria-Kapisa.
  // Bactria proper is a separate centre ~800 km north-west, around Bactra.
  "indo-greek": { label: "Indo-Greek realm", lat: 32.49, lon: 74.53, region: true, dy: 14 },
};

// Mints that exist but cannot be placed; counted and reported, never dropped silently.
const UNLOCATABLE = new Set(["mint illegible"]);

const mintKey = (raw: string) => {
  const lower = raw.toLowerCase().trim();
  if (UNLOCATABLE.has(lower)) return null;
  // Region aliases are matched on the raw string, before the comma-stripping
  // below would reduce "Uncertain mint, Baktria" to "uncertain mint".
  if (lower.includes("baktria")) return "baktria";
  if (lower.includes("indo-greek")) return "indo-greek";
  const v = lower
    .replace(/\(.*?\)/g, " ")       // "Lugdunum (Lyons)" -> "Lugdunum"
    .replace(/,.*$/, " ")           // "Siscia, 4th officina" -> "Siscia"
    .replace(/\b\d+(st|nd|rd|th)\b/g, " ")
    .replace(/\bofficina\b/g, " ")
    .trim()
    .replace(/\s+/g, " ");
  if (v === "rhodos") return "rhodes";
  return v.replace(/\s+/g, "-");
};

type MintPin = {
  key: string; label: string; region: boolean; count: number;
  x: number; y: number; dx: number; dy: number;
  category: string; breakdown: string;
};

const mintCoins = new Map<string, (typeof coins)[number][]>();
for (const coin of coins) {
  const key = mintKey(coin.mint);
  // Coins whose mint cannot be read simply have no pin; they remain listed in
  // the cabinet below, so nothing is lost by leaving them off the map.
  if (key === null) continue;
  if (!MINTS[key]) {
    throw new Error(`Cabinet map: no coordinates for mint "${coin.mint}" (key "${key}") on "${coin.title}". Add it to MINTS.`);
  }
  const list = mintCoins.get(key) ?? [];
  list.push(coin);
  mintCoins.set(key, list);
}

const MAP_W = 1000;
const MAP_H = 520;
const MAP_PAD = 46;

const projection = geoConicEqualArea()
  .parallels([32, 46])
  .rotate([-38, 0])
  .fitExtent(
    [[MAP_PAD, MAP_PAD], [MAP_W - MAP_PAD, MAP_H - MAP_PAD]],
    { type: "MultiPoint", coordinates: [...mintCoins.keys()].map((k) => [MINTS[k].lon, MINTS[k].lat]) } as never,
  );
projection.clipExtent([[0, 0], [MAP_W, MAP_H]]);

const graticulePath = (geoPath(projection)(geoGraticule().step([10, 10])()) ?? "").replace(
  /\d+\.\d+/g,
  (n) => String(Math.round(Number(n) * 10) / 10),
);

const landFeature = feature(landTopo as never, (landTopo as never as { objects: { land: unknown } }).objects.land as never);
// Round to 1dp: the full-precision path is several times larger for no visible gain.
const landPath = (geoPath(projection)(landFeature as never) ?? "").replace(/\d+\.\d+/g, (n) => String(Math.round(Number(n) * 10) / 10));

const mintPins: MintPin[] = [...mintCoins.entries()]
  .map(([key, list]) => {
    const m = MINTS[key];
    const [x, y] = projection([m.lon, m.lat]) ?? [0, 0];
    const sorted = [...list].sort((a, b) => yearFor(a) - yearFor(b));
    const counts = new Map<string, number>();
    for (const c of list) counts.set(categoryFor(c), (counts.get(categoryFor(c)) ?? 0) + 1);
    return {
      key, label: m.label, region: Boolean(m.region), count: list.length,
      x: Math.round(x * 10) / 10, y: Math.round(y * 10) / 10,
      dx: m.dx ?? 0, dy: m.dy ?? 0,
      category: categoryFor(sorted[0]),
      breakdown: [...counts.entries()].map(([c, n]) => `${c}: ${n}`).join(" · "),
    };
  })
  .sort((a, b) => b.count - a.count);

// Uniform dots: size-by-count made the Aegean unreadable, and the count is in
// the tooltip anyway. At r=5 the closest two mints (10 units apart) no longer
// overlap at all.
const DOT_R = 5;

// A hit circle wider than the gap to the nearest mint swallows that mint's
// centre and steals its clicks — which is what made Kalchedon, Athens, Kyzikos
// and Pergamon unclickable. Cap every hit radius below that gap.
const nearestGap = (pin: MintPin) => {
  let best = Infinity;
  for (const other of mintPins) {
    if (other.key === pin.key) continue;
    best = Math.min(best, Math.hypot(pin.x - other.x, pin.y - other.y));
  }
  return best;
};
// One circle only steals another's click when it covers that circle's CENTRE,
// i.e. when the radius exceeds the gap — so 0.85 of the gap is both safe and
// noticeably easier to tap on a phone than half of it.
const hitRadiusFor = (pin: MintPin) => Math.max(7, Math.min(18, nearestGap(pin) * 0.85));
const categorySlug = (c: string) => toToken(c);

const groupedCoinsWithAnchors = groupedCoins.map((group) => ({
  ...group,
  anchor: buildAnchor(group.category),
  items: group.items.map((coin) => ({
    ...coin,
    anchor: buildAnchor(`${coin.title}-${coin.type}`),
    mintKey: mintKey(coin.mint) ?? "",
  })),
}));

// Pills need a label that fits on one line; the section headings stay verbose.
const SHORT_LABEL: Record<string, string> = {
  "Classical & Hellenistic Greek": "Classical & Hellenistic",
};

const totalCoins = coins.length;
---

<BaseLayout title="Numismatic Cabinet | Dogus Ural">
  <section class="section">
    <div class="container">
      <h1>Numismatic Cabinet</h1>
      <p class="muted">A curated cabinet focused on Classical Greek tetradrachms and Roman Republic silver denarii.</p>

      <figure class="mint-map">
        <div class="map-scroll">
        <svg viewBox={`0 0 ${MAP_W} ${MAP_H}`} role="img" aria-labelledby="mintmap-title mintmap-desc">
          <title id="mintmap-title">Where the cabinet was struck</title>
          <desc id="mintmap-desc">
            A map from Gaul to the Indus showing {mintPins.length} mints, sized by how many coins each struck.
          </desc>
          <defs>
            {/* Defined once and referenced twice — as the relief's clip and as
                the visible coastline — rather than repeating 44 KB of path. */}
            <path id="land-outline" d={landPath} />
            <clipPath id="land-clip">
              <use href="#land-outline" />
            </clipPath>
          </defs>
          <g class="map-viewport">
            <rect class="map-sea" x="0" y="0" width={MAP_W} height={MAP_H} />
            <path class="map-graticule" d={graticulePath} vector-effect="non-scaling-stroke" />
            {/* Natural Earth shaded relief, reprojected at build time to this
                exact projection, clipped to land so the sea stays flat. */}
            <image
              class="map-relief"
              href="/map/relief.webp"
              x="0"
              y="0"
              width={MAP_W}
              height={MAP_H}
              preserveAspectRatio="none"
              clip-path="url(#land-clip)"
            />
            <use href="#land-outline" class="map-land" vector-effect="non-scaling-stroke" />
          <g class="mint-layer">
            {mintPins.map((pin) => (
              /* Positioned on the outer group, sized on the inner one: the
                 inner group is counter-scaled against the zoom so markers hold
                 a constant screen size. Without that, zooming magnifies the
                 dots along with the map and the crowded Aegean never separates. */
              <g transform={`translate(${pin.x} ${pin.y})`}>
                <a
                  class="mint"
                  href={`#mint-${pin.key}`}
                  data-mint={pin.key}
                  aria-label={`${pin.label} — ${pin.count} coin${pin.count === 1 ? "" : "s"}`}
                >
                  <g class="mint-scale">
                    <title>{pin.label}{pin.region ? " (region)" : ""} — {pin.breakdown}</title>
                    <circle class="mint-hit" cx="0" cy="0" r={hitRadiusFor(pin)} />
                    <circle class={`mint-dot mint-dot--${categorySlug(pin.category)}`} cx="0" cy="0" r={DOT_R} />
                  </g>
                </a>
              </g>
            ))}
          </g>
          {/* Labels paint in their own layer so a neighbouring marker can never
              cover one — SVG has no z-index, only document order. */}
          <g class="label-layer" aria-hidden="true">
            {mintPins.map((pin) => (
              <g transform={`translate(${pin.x} ${pin.y})`}>
                <g class="mint-scale">
                  <text
                    class="mint-label"
                    data-mint={pin.key}
                    x={pin.dx}
                    y={pin.dy + (pin.dy < 0 ? -DOT_R - 3 : DOT_R + 11)}
                    text-anchor="middle"
                  >
                    {pin.label}
                  </text>
                </g>
              </g>
            ))}
          </g>
          </g>
        </svg>
        </div>
        <div class="map-controls">
          <button type="button" id="map-zoom-out" class="map-btn" aria-label="Zoom out" disabled>−</button>
          <button type="button" id="map-zoom-in" class="map-btn" aria-label="Zoom in">+</button>
          <button type="button" id="map-zoom-reset" class="map-btn map-btn--reset" hidden>Reset</button>
        </div>
        <figcaption>
          <span class="map-hint">Tap a mint to see only its coins. Zoom in to separate the crowded ones.</span>
        </figcaption>
      </figure>

      <nav class="period-nav" id="cabinet" aria-label="Filter by period">
        <a class="period is-active" href="#cabinet" data-period="all">
          All <span class="period-count">{totalCoins}</span>
        </a>
        {groupedCoinsWithAnchors.map((group) => (
          <a class="period" href={`#${group.anchor}`} data-period={group.anchor}>
            {SHORT_LABEL[group.category] ?? group.category} <span class="period-count">{group.items.length}</span>
          </a>
        ))}
        <span class="mint-chip" id="mint-chip" hidden>
          <span id="mint-chip-label"></span>
          <button type="button" id="mint-chip-clear" aria-label="Clear mint filter">×</button>
        </span>
      </nav>

      {groupedCoinsWithAnchors.map((group) => (
        <section class="coin-category" id={group.anchor} data-period={group.anchor}>
          <h2>
            {group.category} <span class="category-count">{group.items.length}</span>
          </h2>
          <details class="coin-index">
            <summary>Jump to a coin</summary>
            <ul>
              {group.items.map((coin) => (
                <li>
                  <a href={`#${coin.anchor}`}>
                    {coin.title} — {coin.type} <span class="muted">· {coin.date}</span>
                  </a>
                </li>
              ))}
            </ul>
          </details>
          <ol class="coin-list">
            {group.items.map((coin) => (
              <li class="coin-item" id={coin.anchor} data-mint={coin.mintKey}>
                <h3>
                  {coin.title} - {coin.type}
                </h3>
                <p class="muted">
                  {coin.date} · {coin.mint}
                </p>
                <img
                  src={coin.image}
                  alt={`${coin.title} ${coin.type}`}
                  class="coin-image"
                  width={coin.width}
                  height={coin.height}
                  loading="lazy"
                  decoding="async"
                />
                <p>
                  <strong>Obv:</strong> {coin.obv}
                </p>
                <p>
                  <strong>Rev:</strong> {coin.rev}
                </p>
                <p>
                  <strong>References:</strong> {coin.refs}
                </p>
                {coin.notes ? (
                  <p>
                    <strong>Notes:</strong> {coin.notes}
                  </p>
                ) : null}
                {coin.condition ? (
                  <p>
                    <strong>Condition:</strong> {coin.condition}
                  </p>
                ) : null}
                {coin.weight || coin.diameter ? (
                  <p>
                    <strong>Weight:</strong> {coin.weight} · <strong>Diameter:</strong> {coin.diameter}
                    {coin.axis ? (
                      <>
                        {" "}
                        · <strong>Axis:</strong> {coin.axis}
                      </>
                    ) : null}
                  </p>
                ) : null}
              </li>
            ))}
          </ol>
        </section>
      ))}
    </div>
  </section>

  <script is:inline>
    (() => {
      const nav = document.querySelector(".period-nav");
      if (!nav) return;

      const header = document.querySelector("header");
      const headerHeight = () => (header ? header.offsetHeight : 0);

      // The header wraps to two rows under 900px, so measure rather than guess.
      const syncOffset = () =>
        document.documentElement.style.setProperty("--header-h", `${headerHeight()}px`);
      syncOffset();
      window.addEventListener("resize", syncOffset);

      const pills = [...nav.querySelectorAll(".period")];
      const sections = [...document.querySelectorAll(".coin-category")];

      const apply = (requested) => {
        const known = requested === "all" || sections.some((s) => s.dataset.period === requested);
        const period = known ? requested : "all";

        for (const section of sections) {
          section.hidden = period !== "all" && section.dataset.period !== period;
        }
        for (const pill of pills) {
          const active = pill.dataset.period === period;
          pill.classList.toggle("is-active", active);
          if (active) pill.setAttribute("aria-current", "true");
          else pill.removeAttribute("aria-current");
        }
        return period;
      };

      for (const pill of pills) {
        pill.addEventListener("click", (event) => {
          event.preventDefault();
          const period = apply(pill.dataset.period);

          history.replaceState(
            null,
            "",
            period === "all" ? location.pathname + location.search : `#${period}`,
          );

          // Sections above the selection just disappeared, so the old scroll
          // position no longer means anything. Land on the first visible group.
          const first = sections.find((section) => !section.hidden);
          if (!first) return;
          const top = first.getBoundingClientRect().top + window.scrollY - headerHeight() - nav.offsetHeight - 12;
          window.scrollTo({ top: Math.max(0, top) });
        });
      }

      const isCategory = (value) => sections.some((section) => section.dataset.period === value);
      const fromHash = () => decodeURIComponent(location.hash.slice(1));

      const svgEl = document.querySelector(".mint-map svg");

      // --- pan & zoom ------------------------------------------------------
      // Hand-rolled rather than pulling in d3-zoom: this is the whole feature
      // in a few dozen lines, and the page still ships no mapping library.
      // Everything inside .map-viewport scales together, so marker spacing and
      // hit areas stay in proportion and clicks cannot start colliding.
      const viewport = document.querySelector(".map-viewport");
      const figure = document.querySelector(".mint-map");
      const MIN_K = 1;
      // The relief carries ~2000px across this extent, so it softens past ~2x.
      // 6x is well beyond what is needed to separate the Aegean cluster (mints
      // 9.6 units apart become ~70px apart on a desktop display) and keeps the
      // terrain reading as soft shading rather than obvious pixels.
      const MAX_K = 6;
      let k = 1;
      let tx = 0;
      let ty = 0;

      const svgSize = () => {
        const r = svgEl.getBoundingClientRect();
        return { w: r.width, h: r.height, left: r.left, top: r.top };
      };

      // Keep the map covering its frame: never let panning expose empty space.
      const clamp = () => {
        const { w, h } = svgSize();
        const maxX = 0;
        const minX = w - w * k;
        const maxY = 0;
        const minY = h - h * k;
        tx = Math.min(maxX, Math.max(minX, tx));
        ty = Math.min(maxY, Math.max(minY, ty));
      };

      let syncZoomButtons = () => {};
      const applyTransform = () => {
        clamp();
        const { w } = svgSize();
        const unit = w / 1000; // px per viewBox unit at k = 1
        viewport.setAttribute("transform", `translate(${tx / unit} ${ty / unit}) scale(${k})`);
        svgEl.style.setProperty("--k", String(k));
        figure.classList.toggle("is-zoomed", k > 1);
        syncZoomButtons();
      };

      const zoomAt = (clientX, clientY, factor) => {
        const { left, top } = svgSize();
        const px = clientX - left;
        const py = clientY - top;
        const next = Math.min(MAX_K, Math.max(MIN_K, k * factor));
        if (next === k) return;
        // hold the point under the cursor fixed
        tx = px - (px - tx) * (next / k);
        ty = py - (py - ty) * (next / k);
        k = next;
        if (k === MIN_K) { tx = 0; ty = 0; }
        applyTransform();
      };

      // Buttons rather than the wheel: scrolling the page with the cursor over
      // the map used to zoom it instead, which is the surprising part.
      const zoomCentre = (factor) => {
        const { w, h, left, top } = svgSize();
        zoomAt(left + w / 2, top + h / 2, factor);
      };
      const zoomInBtn = document.getElementById("map-zoom-in");
      const zoomOutBtn = document.getElementById("map-zoom-out");
      const zoomResetBtn = document.getElementById("map-zoom-reset");
      syncZoomButtons = () => {
        if (zoomInBtn) zoomInBtn.disabled = k >= MAX_K - 0.001;
        if (zoomOutBtn) zoomOutBtn.disabled = k <= MIN_K + 0.001;
        if (zoomResetBtn) zoomResetBtn.hidden = k <= MIN_K + 0.001;
      };
      zoomInBtn?.addEventListener("click", () => zoomCentre(1.6));
      zoomOutBtn?.addEventListener("click", () => zoomCentre(1 / 1.6));
      zoomResetBtn?.addEventListener("click", () => {
        k = MIN_K;
        tx = 0;
        ty = 0;
        applyTransform();
      });

      const pointers = new Map();
      let pinchStart = null;

      svgEl.addEventListener("pointerdown", (event) => {
        pointers.set(event.pointerId, event);
        if (pointers.size === 2) {
          const [a, b] = [...pointers.values()];
          pinchStart = { dist: Math.hypot(a.clientX - b.clientX, a.clientY - b.clientY), k };
        }
      });

      svgEl.addEventListener("pointermove", (event) => {
        if (!pointers.has(event.pointerId)) return;
        const prev = pointers.get(event.pointerId);
        pointers.set(event.pointerId, event);

        if (pointers.size === 2 && pinchStart) {
          const [a, b] = [...pointers.values()];
          const dist = Math.hypot(a.clientX - b.clientX, a.clientY - b.clientY);
          const factor = (dist / pinchStart.dist) * (pinchStart.k / k);
          zoomAt((a.clientX + b.clientX) / 2, (a.clientY + b.clientY) / 2, factor);
          return;
        }
        if (pointers.size === 1 && k > 1) {
          event.preventDefault();
          figure.classList.add("is-panning");
          tx += event.clientX - prev.clientX;
          ty += event.clientY - prev.clientY;
          applyTransform();
        }
      });

      const endPointer = (event) => {
        pointers.delete(event.pointerId);
        if (pointers.size < 2) pinchStart = null;
        if (pointers.size === 0) figure.classList.remove("is-panning");
      };
      svgEl.addEventListener("pointerup", endPointer);
      svgEl.addEventListener("pointercancel", endPointer);
      svgEl.addEventListener("pointerleave", endPointer);

      window.addEventListener("resize", applyTransform);

      // --- mint filter -----------------------------------------------------
      // Period and mint are alternative lenses on the same shelf, never
      // combined: intersecting them would strand the reader on an empty page.
      const pins = [...document.querySelectorAll(".mint")];
      const labels = [...document.querySelectorAll(".mint-label")];
      const items = [...document.querySelectorAll(".coin-item")];
      const chip = document.getElementById("mint-chip");
      const chipLabel = document.getElementById("mint-chip-label");
      const chipClear = document.getElementById("mint-chip-clear");
      const isMint = (value) => pins.some((pin) => pin.dataset.mint === value);

      const clearMint = () => {
        for (const item of items) item.hidden = false;
        for (const pin of pins) pin.classList.remove("is-active");
        for (const label of labels) label.classList.remove("is-active");
        if (chip) { chip.hidden = true; chip.classList.remove("is-shown"); }
        for (const section of sections) section.hidden = false;
      };

      const applyMint = (key) => {
        apply("all");                       // drop any period selection first
        let shown = 0;
        for (const item of items) {
          const match = item.dataset.mint === key;
          item.hidden = !match;
          if (match) shown += 1;
        }
        // a section with nothing left in it should go too, heading and all
        for (const section of sections) {
          section.hidden = [...section.querySelectorAll(".coin-item")].every((i) => i.hidden);
        }
        for (const pin of pins) pin.classList.toggle("is-active", pin.dataset.mint === key);
        for (const label of labels) label.classList.toggle("is-active", label.dataset.mint === key);
        const active = pins.find((pin) => pin.dataset.mint === key);
        if (chip && chipLabel && active) {
          chipLabel.textContent = `${active.getAttribute("aria-label")}`;
          chip.hidden = false;
          chip.classList.add("is-shown");
        }
        return shown;
      };

      // The label layer is a sibling of the marker layer, not a child, so CSS
      // cannot reach from a hovered marker to its label — wire it up here.
      const labelFor = (key) => labels.find((label) => label.dataset.mint === key);
      for (const pin of pins) {
        const label = labelFor(pin.dataset.mint);
        if (label) {
          const show = () => label.classList.add("is-hovered");
          const hide = () => label.classList.remove("is-hovered");
          pin.addEventListener("mouseenter", show);
          pin.addEventListener("mouseleave", hide);
          pin.addEventListener("focus", show);
          pin.addEventListener("blur", hide);
        }
      }

      for (const pin of pins) {
        pin.addEventListener("click", (event) => {
          event.preventDefault();
          const key = pin.dataset.mint;
          const already = pin.classList.contains("is-active");
          if (already) {
            clearMint();
            history.replaceState(null, "", location.pathname + location.search);
            return;
          }
          applyMint(key);
          history.replaceState(null, "", `#mint-${key}`);
          const first = sections.find((section) => !section.hidden);
          if (!first) return;
          const top = first.getBoundingClientRect().top + window.scrollY - headerHeight() - nav.offsetHeight - 12;
          window.scrollTo({ top: Math.max(0, top) });
        });
      }

      if (chipClear) {
        chipClear.addEventListener("click", () => {
          clearMint();
          history.replaceState(null, "", location.pathname + location.search);
        });
      }

      // choosing a period must drop the mint lens
      for (const pill of pills) {
        pill.addEventListener("click", () => clearMint(), true);
      }

      // A category hash filters; a coin hash leaves everything shown so the
      // browser's own anchor jump still lands on it.
      const initial = fromHash();
      if (initial.startsWith("mint-") && isMint(initial.slice(5))) applyMint(initial.slice(5));
      else if (initial) apply(initial);

      // Back/forward and hand-edited hashes. Guarded on isCategory so that
      // clicking a coin link doesn't silently reset the active filter.
      window.addEventListener("hashchange", () => {
        const next = fromHash();
        if (next.startsWith("mint-") && isMint(next.slice(5))) applyMint(next.slice(5));
        else if (isCategory(next)) { clearMint(); apply(next); }
      });
    })();
  </script>
</BaseLayout>

<style>
  /* Period filter bar. Sticks below the site header; --header-h is measured
     at runtime because the header wraps to two rows on narrow screens. */
  .period-nav {
    position: sticky;
    top: var(--header-h, 72px);
    z-index: 20;
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    margin: 1.4rem -0.5rem 1.8rem;
    padding: 0.6rem 0.5rem;
    background: color-mix(in srgb, var(--canvas) 92%, transparent);
    backdrop-filter: saturate(140%) blur(8px);
    border-bottom: 1px solid var(--hairline);
    scroll-margin-top: 6.5rem;
  }

  .period {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.35rem 0.8rem;
    border: 1px solid var(--hairline);
    border-radius: var(--rounded-pill);
    background: var(--surface-card);
    color: var(--muted);
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    white-space: nowrap;
    flex-shrink: 0;
    transition: color 0.15s ease, border-color 0.15s ease, background-color 0.15s ease;
  }

  .period:hover {
    color: var(--ink);
    border-color: var(--ink);
  }

  .period.is-active {
    background: var(--ink);
    border-color: var(--ink);
    color: var(--canvas);
  }

  .period-count {
    font-variant-numeric: tabular-nums;
    font-size: 0.75rem;
    opacity: 0.65;
  }

  .category-count {
    display: inline-block;
    margin-left: 0.35rem;
    padding: 0.1rem 0.5rem;
    border-radius: var(--rounded-pill);
    background: var(--surface-card);
    color: var(--muted);
    font-family: var(--font-body);
    font-size: 0.8rem;
    font-weight: 500;
    font-variant-numeric: tabular-nums;
    vertical-align: middle;
  }

  /* Collapsed by default so 18 links don't push the coins off-screen. */
  .coin-index {
    margin: 0 0 1.2rem;
    border: 1px solid var(--hairline);
    border-radius: var(--rounded-md);
    background: var(--surface-card);
  }

  .coin-index summary {
    padding: 0.55rem 0.9rem;
    cursor: pointer;
    font-size: 0.88rem;
    font-weight: 500;
    color: var(--muted);
    list-style: none;
  }

  .coin-index summary::-webkit-details-marker {
    display: none;
  }

  .coin-index summary::before {
    content: "▸";
    display: inline-block;
    margin-right: 0.5rem;
    transition: transform 0.15s ease;
  }

  .coin-index[open] summary::before {
    transform: rotate(90deg);
  }

  .coin-index summary:hover {
    color: var(--ink);
  }

  .coin-index ul {
    margin: 0;
    padding: 0 0.9rem 0.8rem 1.9rem;
    display: grid;
    gap: 0.4rem;
    list-style: none;
  }

  .coin-index a {
    font-size: 0.87rem;
    color: var(--body);
    text-decoration: none;
  }

  .coin-index a:hover {
    color: var(--ink);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .coin-category[hidden] {
    display: none;
  }

  .map-controls {
    position: absolute;
    top: 0.6rem;
    right: 0.6rem;
    display: flex;
    gap: 0.3rem;
    z-index: 2;
  }

  .map-btn {
    width: 30px;
    height: 30px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--hairline);
    border-radius: var(--rounded-sm);
    background: color-mix(in srgb, var(--surface-card) 88%, transparent);
    backdrop-filter: blur(4px);
    color: var(--ink);
    font-size: 1rem;
    font-weight: 600;
    line-height: 1;
    cursor: pointer;
    transition: border-color 0.15s ease, color 0.15s ease;
  }

  .map-btn--reset {
    width: auto;
    padding: 0 0.6rem;
    font-size: 0.78rem;
    font-weight: 500;
  }

  .map-btn:hover:not(:disabled) {
    border-color: var(--ink);
  }

  .map-btn:disabled {
    opacity: 0.35;
    cursor: default;
  }

  /* display:inline-flex above outranks the UA stylesheet's [hidden] rule, so
     the reset button would otherwise stay visible at rest. */
  .map-btn[hidden] {
    display: none;
  }

  .mint-map {
    position: relative;
    margin: 1.6rem 0 0;
    padding: 0;
    border: 1px solid var(--hairline);
    border-radius: var(--rounded-lg);
    background: var(--surface-card);
    overflow: hidden;
  }

  /* The map is 2.3:1, so on a phone it would render ~150px tall and the smaller
     mints would be 2px wide. Give it a floor and let it pan instead. */
  .map-scroll {
    overflow-x: auto;
    overscroll-behavior-x: contain;
  }

  .mint-map svg {
    display: block;
    width: 100%;
    min-width: 660px;
    height: auto;
  }

  .mint-hit {
    fill: transparent;
  }

  .map-sea {
    fill: var(--surface-card);
  }

  .map-graticule {
    fill: none;
    stroke: var(--hairline);
    stroke-width: 0.4;
    opacity: 0.55;
  }

  /* The relief supplies the land tone, so the outline only has to draw the
     coast. non-scaling-stroke keeps it hairline-crisp at every zoom level. */
  .map-land {
    fill: none;
    stroke: color-mix(in srgb, var(--muted) 80%, transparent);
    stroke-width: 0.9;
    stroke-linejoin: round;
    stroke-linecap: round;
  }

  .map-relief {
    opacity: 0.85;
  }

  /* The relief ships as light-on-white; invert it for the dark palette so
     ridges read correctly instead of glowing. */
  :global(html[data-theme="dark"]) .map-relief {
    filter: invert(1) brightness(0.62) contrast(1.05);
    opacity: 0.55;
  }

  .map-viewport {
    transform-origin: 0 0;
  }

  /* Undo the viewport zoom for markers and labels only. --k is set by the
     pan/zoom code; the fallback keeps them correct before it runs. */
  .mint-scale {
    transform: scale(calc(1 / var(--k, 1)));
  }

  .mint-map.is-zoomed .map-scroll {
    cursor: grab;
  }

  .mint-map.is-panning .map-scroll {
    cursor: grabbing;
  }

  .mint {
    cursor: pointer;
  }

  .mint-dot {
    stroke: var(--surface-card);
    stroke-width: 1.2;
    transition: r 0.12s ease, stroke-width 0.12s ease;
  }

  .mint:hover .mint-dot,
  .mint:focus .mint-dot {
    r: 7.5;
    stroke-width: 2;
  }

  .mint-dot--archaic-greek { fill: var(--brand-ochre); }
  .mint-dot--classical-hellenistic-greek { fill: var(--brand-teal); }
  .mint-dot--greco-baktrian-indo-greek { fill: var(--brand-pink); }
  .mint-dot--roman-imperial { fill: var(--brand-coral); }
  .mint-dot--islamic { fill: var(--brand-mint); }
  .mint-dot--medieval { fill: var(--brand-lavender); }

  /* Twenty-three labels will not all fit; the busiest mints stay lit and the
     rest surface on hover or keyboard focus. */
  /* Every mint behaves the same: named on hover or focus, and while selected.
     No standing labels, so the map stays clean and nothing looks arbitrary. */
  .mint-label {
    font-family: var(--font-body);
    font-size: 12px;
    font-weight: 600;
    fill: var(--ink);
    paint-order: stroke;
    stroke: var(--surface-card);
    stroke-width: 3;
    stroke-linejoin: round;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.15s ease;
  }

  .mint-label.is-hovered,
  .mint-label.is-active {
    opacity: 1;
  }

  .mint.is-active .mint-dot {
    r: 7.5;
    stroke: var(--ink);
    stroke-width: 2;
  }

  .mint-map figcaption {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem 1rem;
    padding: 0.7rem 1rem 0.9rem;
    border-top: 1px solid var(--hairline);
    font-size: 0.85rem;
    color: var(--muted);
  }

  /* Active mint chip in the period bar */
  .mint-chip {
    display: none;
    align-items: center;
    gap: 0.45rem;
    padding: 0.35rem 0.5rem 0.35rem 0.8rem;
    border-radius: var(--rounded-pill);
    border: 1px solid var(--ink);
    background: var(--ink);
    color: var(--canvas);
    font-size: 0.85rem;
    font-weight: 500;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .mint-chip.is-shown {
    display: inline-flex;
  }

  .mint-chip button {
    all: unset;
    cursor: pointer;
    line-height: 1;
    padding: 0 0.25rem;
    font-size: 1rem;
    opacity: 0.75;
  }

  .mint-chip button:hover {
    opacity: 1;
  }

  .coin-item[hidden] {
    display: none;
  }

  .coin-category {
    margin-top: 1.3rem;
    scroll-margin-top: 6.5rem;
  }

  .coin-category h2 {
    margin-bottom: 0.55rem;
  }

  .coin-list {
    margin: 0;
    padding-left: 1.25rem;
    display: grid;
    gap: 1.4rem;
  }

  .coin-item {
    border-bottom: 1px solid var(--hairline);
    padding-bottom: 1.6rem;
    margin-bottom: 0.35rem;
    scroll-margin-top: 6.5rem;
  }

  .coin-item p:last-child {
    margin-bottom: 0.15rem;
  }

  .coin-image {
    width: min(760px, 100%);
    height: auto;
    display: block;
    border: 1px solid var(--hairline);
    border-radius: var(--rounded-md);
    background: var(--surface-soft);
    margin: 0.7rem 0 0.9rem;
  }

  @media (max-width: 720px) {
    .mint-label {
      font-size: 15px;
    }

    /* Wrapping to three rows costs ~126px of a small viewport on top of an
       already-tall wrapped header, so scroll the pills sideways instead. */
    .period-nav {
      flex-wrap: nowrap;
      overflow-x: auto;
      scrollbar-width: none;
      gap: 0.35rem;
      margin-bottom: 1.4rem;
      scroll-padding-left: 0.5rem;
      overscroll-behavior-x: contain;
    }

    .period-nav::-webkit-scrollbar {
      display: none;
    }

    .period {
      padding: 0.3rem 0.65rem;
      font-size: 0.8rem;
    }
  }

  /* The bar's own sticky offset is enough; don't double it on the sections. */
  @media (prefers-reduced-motion: reduce) {
    .period,
    .coin-index summary::before {
      transition: none;
    }
  }
</style>
